name: valence
description: A starting point for Dart libraries or applications.
version: 1.0.0

environment:
  sdk: ^3.11.4

dependencies:
  meta: ^1.18.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
