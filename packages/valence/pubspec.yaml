name: valence
description: High-performance reactive state for Dart & Flutter.
version: 1.0.0

environment:
  sdk: ^3.11.1

resolution: workspace

dependencies:
  meta: ^1.18.1
  # path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
