name: benchmark
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.11.3

# Add regular dependencies here.
dependencies:
  benchmark_harness: ^2.4.0
  bloc: ^9.2.0
  path: ^1.9.0
  signals_core: ^6.2.1
  valence:
    path: ../../packages/valence

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
